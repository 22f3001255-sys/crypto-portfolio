## Round 2 Update

Upgrade the crypto tracker: add a total portfolio value display (#total-value), sorting by price (#sort-price), filtering by gain/loss (#filter-gain-loss), and a dark/light theme toggle (#theme-toggle) persisting in localStorage. Also, show historical price chart (#price-chart) using Chart.js and include a loading spinner (#loading-spinner). Ensure mobile responsiveness and correct IDs for checks.

This round adds all new features, design tweaks, and logic changes described in the brief.